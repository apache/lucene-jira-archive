--- HTMLParser.jj.bak	2003-10-29 10:29:31.000000000 +0100
+++ HTMLParser.jj	2003-10-29 13:44:04.000000000 +0100
@@ -246,6 +246,13 @@
 }
 {
   t1=<TagName> {
+    String tagName = t1.image.toLowerCase();
+    if( tagName.equals("<p") || tagName.equals("<div") ||
+    	tagName.equals("<br") || tagName.equals("<br/") ||
+    	tagName.equals("<hr") || tagName.equals("<hr/") ||
+    	tagName.equals("<td") ) {
+    	addSpace();
+    }
     inTitle = t1.image.equalsIgnoreCase("<title"); // keep track if in <TITLE>
     inMetaTag = t1.image.equalsIgnoreCase("<META"); // keep track if in <META>
     inStyle = t1.image.equalsIgnoreCase("<STYLE"); // keep track if in <STYLE>
