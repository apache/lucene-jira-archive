diff --git a/lucene/common-build.xml b/lucene/common-build.xml
index 0027884..d184f7f 100644
--- a/lucene/common-build.xml
+++ b/lucene/common-build.xml
@@ -738,6 +738,8 @@
             seed="${tests.seed}"
 
             heartbeat="${tests.heartbeat}"
+
+            uniqueSuiteNames="false"
         >
             <!-- Classpaths. -->
             <classpath refid="@{junit.classpath}"/>
@@ -895,11 +897,18 @@
             </listeners>
 
             <!-- Input test classes. -->
-            <fileset dir="@{testsDir}">
+            <fileset id="tests.fileset" dir="@{testsDir}">
               <include name="**/Test*.class" />
               <include name="**/*Test.class" />
               <exclude name="**/*$*" />
             </fileset>
+            <!-- add #cores - 1 repetitions here (you may want to decrease a bit if they're multi-threaded so that you 
+                 don't kill your test machine :) -->
+            <fileset refid="tests.fileset" />
+            <fileset refid="tests.fileset" />
+            <fileset refid="tests.fileset" />
+            <fileset refid="tests.fileset" />
+            <fileset refid="tests.fileset" />
         </junit4:junit4>
 
         <!-- Report the 5 slowest tests from this run to the console. -->
