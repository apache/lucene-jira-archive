Index: src/java/org/apache/lucene/analysis/PerFieldAnalyzerWrapper.java
===================================================================
--- src/java/org/apache/lucene/analysis/PerFieldAnalyzerWrapper.java	(revision 804059)
+++ src/java/org/apache/lucene/analysis/PerFieldAnalyzerWrapper.java	(working copy)
@@ -54,9 +54,27 @@
    * defined to use a different analyzer will use the one provided here.
    */
   public PerFieldAnalyzerWrapper(Analyzer defaultAnalyzer) {
+    this(defaultAnalyzer, null);
+  }
+  
+  /**
+   * Constructs with default analyzer and a list of analyzers to use for 
+   * specific fields.
+   *
+   * @param defaultAnalyzer Any fields not specifically
+   * defined to use a different analyzer will use the one provided here.
+   * @param fieldAnalyzers a Map of field name Strings to the Analyzers to be 
+   * used for those fields 
+   */
+  public PerFieldAnalyzerWrapper(Analyzer defaultAnalyzer, 
+      Map /*<String, Analyzer>*/ fieldAnalyzers) {
     this.defaultAnalyzer = defaultAnalyzer;
+    if (fieldAnalyzers != null) {
+      analyzerMap.putAll(fieldAnalyzers);
+    }
     setOverridesTokenStreamMethod(PerFieldAnalyzerWrapper.class);
   }
+  
 
   /**
    * Defines an analyzer to use for the specified field.
