Index: lucene/contrib/spatial/src/test/org/apache/lucene/spatial/tier/TestCartesian.java
===================================================================
--- lucene/contrib/spatial/src/test/org/apache/lucene/spatial/tier/TestCartesian.java	(révision 933864)
+++ lucene/contrib/spatial/src/test/org/apache/lucene/spatial/tier/TestCartesian.java	(copie de travail)
@@ -486,8 +486,8 @@
   public void testRange() throws IOException, InvalidGeoException {
     searcher = new IndexSearcher(directory, true);
 
-    final double[] milesToTest = new double[] {6.0, 0.5, 0.001, 0.0};
-    final int[] expected = new int[] {7, 1, 0, 0};
+    final double[] milesToTest = new double[] {6.0, 0.5, 0.001, 0.0, 100000.0};
+    final int[] expected = new int[] {7, 1, 0, 0, 23};
 
     for(int x=0;x<expected.length;x++) {
     
@@ -571,7 +571,7 @@
         if (VERBOSE) System.out.println("Name: "+ name +", Distance "+ distance); //(res, ortho, harvesine):"+ distance +" |"+ geo_distance +"|"+ llm +" | score "+ hits.score(i));
         assertTrue(Math.abs((distance - llm)) < 1);
         assertTrue((distance < miles ));
-        assertTrue(geo_distance > lastDistance);
+        assertTrue(geo_distance >= lastDistance);
         lastDistance = geo_distance;
       }
     }
