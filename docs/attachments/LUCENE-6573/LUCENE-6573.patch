Index: lucene/benchmark/build.xml
===================================================================
--- lucene/benchmark/build.xml	(revision 1687065)
+++ lucene/benchmark/build.xml	(working copy)
@@ -175,6 +175,8 @@
       <pathelement path="${queries.jar}"/>
       <pathelement path="${codecs.jar}"/>
       <pathelement path="${join.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
       <path refid="base.classpath"/>
       <fileset dir="lib"/>
     </path>
Index: lucene/classification/build.xml
===================================================================
--- lucene/classification/build.xml	(revision 1687065)
+++ lucene/classification/build.xml	(working copy)
@@ -27,6 +27,8 @@
   <path id="classpath">
     <path refid="base.classpath"/>
     <pathelement path="${queries.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
   </path>
 
   <path id="test.classpath">
@@ -33,6 +35,8 @@
     <pathelement path="${analyzers-common.jar}"/>
     <pathelement location="${codecs.jar}"/>
     <path refid="test.base.classpath"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
   </path>
 
   <target name="compile-core" depends="jar-queries,jar-analyzers-common,common.compile-core" />
Index: lucene/expressions/build.xml
===================================================================
--- lucene/expressions/build.xml	(revision 1687065)
+++ lucene/expressions/build.xml	(working copy)
@@ -30,6 +30,8 @@
     <path refid="base.classpath"/>
     <fileset dir="lib"/>
     <pathelement path="${queries.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
   </path>
 
   <path id="test.classpath">
@@ -36,6 +38,8 @@
     <path refid="test.base.classpath"/>
     <fileset dir="lib"/>
     <pathelement path="src/test-files"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
   </path>
 
   <target name="compile-core" depends="jar-queries,common.compile-core" />
Index: lucene/grouping/build.xml
===================================================================
--- lucene/grouping/build.xml	(revision 1687065)
+++ lucene/grouping/build.xml	(working copy)
@@ -28,11 +28,15 @@
     <path id="test.classpath">
       <path refid="test.base.classpath" />
       <pathelement path="${queries.jar}" />
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     </path>
 
     <path id="classpath">
       <pathelement path="${queries.jar}" />
       <path refid="base.classpath"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     </path>
 
     <target name="init" depends="module-build.init,jar-queries"/>
Index: lucene/highlighter/build.xml
===================================================================
--- lucene/highlighter/build.xml	(revision 1687065)
+++ lucene/highlighter/build.xml	(working copy)
@@ -33,6 +33,8 @@
     <pathelement path="${queries.jar}"/>
     <pathelement path="${join.jar}"/>
     <pathelement path="${analyzers-common.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="base.classpath"/>
   </path>
 
Index: lucene/join/build.xml
===================================================================
--- lucene/join/build.xml	(revision 1687065)
+++ lucene/join/build.xml	(working copy)
@@ -26,6 +26,8 @@
 
   <path id="classpath">
     <pathelement path="${grouping.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="base.classpath"/>
   </path>
 
Index: lucene/memory/build.xml
===================================================================
--- lucene/memory/build.xml	(revision 1687065)
+++ lucene/memory/build.xml	(working copy)
@@ -27,6 +27,8 @@
 
   <path id="test.classpath">
     <pathelement path="${queryparser.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="test.base.classpath"/>
   </path>
 
Index: lucene/misc/build.xml
===================================================================
--- lucene/misc/build.xml	(revision 1687065)
+++ lucene/misc/build.xml	(working copy)
@@ -23,6 +23,9 @@
     Index tools and other miscellaneous code
   </description>
 
+  <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+       as few dependencies as possible if we want them to remain manageable. -->
+
   <import file="../module-build.xml"/>
 
   <target name="install-cpptasks" unless="cpptasks.uptodate" depends="ivy-availability-check,ivy-fail,ivy-configure">
Index: lucene/queries/build.xml
===================================================================
--- lucene/queries/build.xml	(revision 1687065)
+++ lucene/queries/build.xml	(working copy)
@@ -21,4 +21,7 @@
   </description>
 
   <import file="../module-build.xml"/>
+
+  <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+       as few dependencies as possible if we want them to remain manageable. -->
 </project>
Index: lucene/queryparser/build.xml
===================================================================
--- lucene/queryparser/build.xml	(revision 1687065)
+++ lucene/queryparser/build.xml	(working copy)
@@ -25,6 +25,8 @@
   <path id="classpath">
     <pathelement path="${queries.jar}"/>
     <pathelement path="${sandbox.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="base.classpath"/>
   </path>
 
Index: lucene/replicator/build.xml
===================================================================
--- lucene/replicator/build.xml	(revision 1687065)
+++ lucene/replicator/build.xml	(working copy)
@@ -29,6 +29,8 @@
   <path id="classpath">
   	<fileset dir="lib" />
     <pathelement path="${facet.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="base.classpath"/>
   </path>
 
Index: lucene/sandbox/build.xml
===================================================================
--- lucene/sandbox/build.xml	(revision 1687065)
+++ lucene/sandbox/build.xml	(working copy)
@@ -25,6 +25,8 @@
 
   <path id="classpath">
     <fileset dir="lib"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="base.classpath"/>
   </path>
 
Index: lucene/spatial/build.xml
===================================================================
--- lucene/spatial/build.xml	(revision 1687065)
+++ lucene/spatial/build.xml	(working copy)
@@ -33,6 +33,8 @@
     <path refid="spatialjar"/>
     <pathelement path="${queries.jar}" />
     <pathelement path="${misc.jar}" />
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
   </path>
 
   <path id="test.classpath">
@@ -39,6 +41,8 @@
     <path refid="test.base.classpath" />
     <path refid="spatialjar"/>
     <pathelement path="src/test-files" />
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
   </path>
 
   <target name="compile-core" depends="jar-queries,jar-misc,common.compile-core" />
Index: lucene/suggest/build.xml
===================================================================
--- lucene/suggest/build.xml	(revision 1687065)
+++ lucene/suggest/build.xml	(working copy)
@@ -32,6 +32,8 @@
     <pathelement path="${analyzers-common.jar}"/>
     <pathelement path="${misc.jar}"/>
     <pathelement path="${queries.jar}"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="base.classpath"/>
   </path>
 
Index: lucene/test-framework/build.xml
===================================================================
--- lucene/test-framework/build.xml	(revision 1687065)
+++ lucene/test-framework/build.xml	(working copy)
@@ -27,6 +27,8 @@
   <path id="classpath">
     <pathelement location="${common.dir}/build/core/classes/java"/>
     <pathelement location="${common.dir}/build/codecs/classes/java"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <fileset dir="lib"/>
   </path>
 
@@ -33,6 +35,8 @@
   <path id="test.classpath"> 
     <pathelement location="${build.dir}/classes/java"/>
     <pathelement location="${build.dir}/classes/test"/>
+    <!-- IMPORTANT: Do not add more module dependencies here: modules must have
+         as few dependencies as possible if we want them to remain manageable. -->
     <path refid="classpath"/>
     <path refid="junit-path"/>
   </path>
