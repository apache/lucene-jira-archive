Index: build.xml
===================================================================
--- build.xml	(revision 1696037)
+++ build.xml	(working copy)
@@ -505,6 +505,12 @@
         <not><equals arg1="${java.specification.version}" arg2="1.8"/></not>
       </condition>
     </fail>
+    <fail message="To run nightly smoke, you must use ANT version 1.8.x. Yours: ${ant.version}">
+      <condition>
+        <!-- minimum ANT version is already checked by common-build.xml! -->
+        <antversion atleast="1.9.0" />
+      </condition>
+    </fail>
     <exec executable="${python32.exe}" failonerror="true" taskname="python32">
       <arg value="-V"/>
     </exec>
