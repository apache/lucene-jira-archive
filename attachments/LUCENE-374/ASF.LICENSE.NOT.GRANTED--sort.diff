Index: /home/dnaber/workspace/LuceneSVN/src/java/org/apache/lucene/search/FieldCacheImpl.java
===================================================================
--- /home/dnaber/workspace/LuceneSVN/src/java/org/apache/lucene/search/FieldCacheImpl.java	(Revision 160064)
+++ /home/dnaber/workspace/LuceneSVN/src/java/org/apache/lucene/search/FieldCacheImpl.java	(Arbeitskopie)
@@ -253,12 +253,9 @@
         mterms[t++] = null;
 
         try {
-          if (termEnum.term() == null) {
-            throw new RuntimeException ("no terms in field " + field);
-          }
           do {
             Term term = termEnum.term();
-            if (term.field() != field) break;
+            if (term == null || term.field() != field) break;
 
             // store term text
             // we expect that there is at most one term per document
