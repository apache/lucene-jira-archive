Index: SpanishLightStemmer.java
===================================================================
--- SpanishLightStemmer.java	(revision 1729187)
+++ SpanishLightStemmer.java	(working copy)
@@ -62,9 +62,6 @@
 public class SpanishLightStemmer {
   
   public int stem(char s[], int len) {
-    if (len < 5)
-      return len;
-    
     for (int i = 0; i < len; i++)
       switch(s[i]) {
         case 'à': 
@@ -88,12 +85,17 @@
         case 'î':
         case 'ï': s[i] = 'i'; break;
       }
-    
+
+	if (len < 4 || (len < 5 && s[len-1] == 's'))
+	  return len;
+
     switch(s[len-1]) {
       case 'o':
       case 'a':
       case 'e': return len - 1;
       case 's':
+        if (s[len-2] == 'o' || s[len-2] == 'a' || s[len-2] == 'e')
+          return len-2;
         if (s[len-2] == 'e' && s[len-3] == 's' && s[len-4] == 'e')
           return len-2;
         if (s[len-2] == 'e' && s[len-3] == 'c') {
@@ -100,8 +102,6 @@
           s[len-3] = 'z';
           return len - 2;
         }
-        if (s[len-2] == 'o' || s[len-2] == 'a' || s[len-2] == 'e')
-          return len - 2;
     }
     
     return len;
