Index: TestIndexReader.java
===================================================================
RCS file: /home/cvspublic/jakarta-lucene/src/test/org/apache/lucene/index/TestIndexReader.java,v
retrieving revision 1.8
diff -u -r1.8 TestIndexReader.java
--- TestIndexReader.java	1 Aug 2004 22:55:12 -0000	1.8
+++ TestIndexReader.java	10 Aug 2004 20:58:42 -0000
@@ -83,7 +83,7 @@
         // verify fields again
         reader = IndexReader.open(d);
         fieldNames = reader.getFieldNames();
-        assertEquals(9, fieldNames.size());    // the following fields + an empty one (bug?!)
+        assertEquals(8, fieldNames.size());    // the following fields + an empty one (bug?!)
         assertTrue(fieldNames.contains("keyword"));
         assertTrue(fieldNames.contains("text"));
         assertTrue(fieldNames.contains("unindexed"));
@@ -105,7 +105,7 @@
 
         // verify that only unindexed fields were returned
         Collection unindexedFieldNames = reader.getFieldNames(false);
-        assertEquals(3, unindexedFieldNames.size());    // the following fields + an empty one
+        assertEquals(2, unindexedFieldNames.size());    // the following fields + an empty one
         assertTrue(unindexedFieldNames.contains("unindexed"));
         assertTrue(unindexedFieldNames.contains("unindexed2"));
     }
