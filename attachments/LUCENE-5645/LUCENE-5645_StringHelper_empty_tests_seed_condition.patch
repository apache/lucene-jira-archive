Index: lucene/core/src/java/org/apache/lucene/util/StringHelper.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- lucene/core/src/java/org/apache/lucene/util/StringHelper.java	(revision 1598395)
+++ lucene/core/src/java/org/apache/lucene/util/StringHelper.java	(revision )
@@ -158,10 +158,14 @@
     if (prop != null) {
       // So if there is a test failure that relied on hash
       // order, we remain reproducible based on the test seed:
-      if (prop.length() > 8) {
-        prop = prop.substring(prop.length()-8);
+      int newSeed;
+      try {
+        newSeed = Integer.parseInt(prop.substring(0, 8), 16);
+      } catch (Exception e) {
+        //fallback on hashCode
+        newSeed = prop.hashCode();
       }
-      GOOD_FAST_HASH_SEED = (int) Long.parseLong(prop, 16);
+      GOOD_FAST_HASH_SEED = newSeed;
     } else {
       GOOD_FAST_HASH_SEED = (int) System.currentTimeMillis();
     }
