Index: lucene/common-build.xml
===================================================================
--- lucene/common-build.xml	(revision 1394824)
+++ lucene/common-build.xml	(working copy)
@@ -1143,6 +1143,20 @@
     <test-macro threadNum="${tests.jvms}" />
   </target>
 
+  <target name="beast" depends="resolve-groovy" description="Repeats runs unit tests">
+    <fail unless="beast.count">You must set the property 'beast.count'!</fail>
+    <script language="groovy" classpathref="groovy.classpath"><![CDATA[
+      int count = Integer.parseInt(project.getProperty('beast.count'));
+      for (int i = 0; i < count; i++) {
+        def task = project.createTask('antcall');
+        task.setTarget('test');
+        task.setInheritAll(true);
+        task.setInheritRefs(true);
+        task.perform();
+      }
+    ]]></script>
+  </target>
+
   <!--
    See http://issues.apache.org/jira/browse/LUCENE-721
    -->
