diff --git a/build.gradle b/build.gradle
index f64eff2b0d8..89d9141c71b 100644
--- a/build.gradle
+++ b/build.gradle
@@ -53,6 +53,39 @@ allprojects {
   version = rootProject.version
 }
 
+allprojects {
+  configurations {
+    jarValidation
+  }
+
+  // For Java projects, add all dependencies from the following configurations
+  // to jar validation
+  plugins.withType(JavaPlugin) {
+    configurations {
+      jarValidation {
+        extendsFrom runtimeClasspath
+        extendsFrom compileClasspath
+        extendsFrom testRuntimeClasspath
+        extendsFrom testCompileClasspath
+      }
+    }
+  }
+
+  afterEvaluate {
+    def allCollected = [
+        configurations.jarValidation,
+        *configurations.jarValidation.extendsFrom.toArray()
+    ]
+
+    configurations.matching { Configuration config ->
+      return !(config in allCollected)
+    }.all { Configuration config ->
+      logger.lifecycle("Project ${project.path}, config: ${config}")
+      config.resolutionStrategy.disableDependencyVerification()
+    }
+  }
+}
+
 ext {
   // "base" version is stripped of the qualifier. Compute it.
   baseVersion = {
