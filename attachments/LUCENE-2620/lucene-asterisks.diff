--- src/java/org/apache/lucene/search/WildcardTermEnum.java.orig	2010-02-14 22:46:40.000000000 +0100
+++ src/java/org/apache/lucene/search/WildcardTermEnum.java	2010-05-24 18:47:08.000000000 +0200
@@ -168,8 +168,11 @@
         //
         if (pattern.charAt(p) == WILDCARD_STRING)
         {
-          // Look at the character beyond the '*'.
-          ++p;
+          // Look at the character beyond the '*' character(s).
+          while (pattern.charAt(p) == WILDCARD_STRING)
+          {
+            ++p;
+          }
           // Examine the string, starting at the last character.
           for (int i = string.length(); i >= s; --i)
           {
