Index: solr/example/example-DIH/solr/db/conf/solrconfig.xml
===================================================================
--- solr/example/example-DIH/solr/db/conf/solrconfig.xml	(revision 1597314)
+++ solr/example/example-DIH/solr/db/conf/solrconfig.xml	(working copy)
@@ -238,9 +238,24 @@
          can perform merges in the background using separate threads.
          The SerialMergeScheduler (Lucene 2.2 default) does not.
      -->
-    <!-- 
-       <mergeScheduler class="org.apache.lucene.index.ConcurrentMergeScheduler"/>
-       -->
+    <!--
+         When using the dataimport handler with a JDBC datasource, it is
+         critical to increase the maximum number of merges that can be
+         scheduled simultaneously with the maxMergeCount parameter.  If this
+         is not done, a large segment merge will result in the indexing thread
+         pausing long enough for the JDBC connection to time out, which will
+         abort the import with an error.
+         
+         The maxThreadCount parameter should be left at 1 in most cases.  With
+         standard hard disks, multiple threads will likely result in I/O
+         contention.  If the index is stored on Solid State Disk and there
+         are plenty of CPU cores available to handle additional threads, this
+         parameter can be safely increased by a small amount.
+     -->
+    <mergeScheduler class="org.apache.lucene.index.ConcurrentMergeScheduler">
+      <maxMergeCount>8</maxMergeCount>
+      <maxThreadCount>1</maxThreadCount>
+    </mergeScheduler>
 
     <!-- LockFactory 
 
